# figgen — AIの入口

説明図を作る道具。使い方は [README.md](README.md)、**YAML方言の正本は
[`src/figgen/BLOCKS.md`](src/figgen/BLOCKS.md)**（キー・記法・落とし穴・禁じ手）。
ここはAIがこのリポジトリで作業に入るときの注意だけ。

MCP 経由（`figgen_blocks`）で返るのも BLOCKS.md と同じ中身なので、
方言の話はそちらを直す。ここには書き写さない。

## 何をする道具か

YAMLの仕様 → HTML/CSS → Chrome で描画 → PNG。画像生成モデルは使わない。文字は焼かない。

## 図を頼まれたときの手順

1. **まず既存の仕様を見る。** プロジェクト内に似た図があれば真似る（`examples/` にも4枚ある）。
   形が揃っていないと、並べたときに読み手が別の図だと思う
2. 仕様のYAMLを、**その図を使うプロジェクト側**に書く。この道具のディレクトリには置かない
3. `python draw.py <spec>.yaml` で出す（pip で入れてあるなら `figgen <spec>.yaml`）
4. **必ず出たPNGを開いて見る。** CSSは検証しないので、はみ出し・重なりは目で見るしかない
5. 直すのは仕様のYAMLか `theme.css`。生成された `.html` と `.png` は生成物なので手で編集しない

## やってはいけないこと

- 図の中に、資料側で確かめていない数字を書かない。仕様に書いた数字は必ず一次資料か実測に接地させ、
  `source:` に出典を書く
- **数えられるものは仕様に手で書かない。** データから出す（`graph_pair` の caption を省くと自動で出る）。
  手で書くと、データを更新したときに数字だけ古いまま残る
- **囲い（凸包）でグループを表さない。** 凸包はそのグループに属さない点まで囲ってしまい、読み手に嘘をつく。
  一度これで失敗している（`図プロンプト3_意義と分野のグルーピング.md` に経緯）
- ブロックを増やす前に、既存のブロックの組み合わせで済まないか考える。増やすなら `blocks.py` に
  関数を1つ足して `RENDERERS` に登録し、見た目は `theme.css` 側に書く（HTMLの中にstyleを書かない）
- 色を足すなら dataviz の `validate_palette.js` を通す。目で見て決めない
- 横にあふれても警告が出ない。列を増やすときは特に、出力を見て確かめる

## 詰まったとき

- 描画に失敗する → Chrome の場所。`FIGGEN_CHROME` で指定できる
- 縦にあふれる警告が出る → 中身を減らすか2枚に割る。`height` を大きくして誤魔化さない
- 文字化けする → 仕様のYAMLはUTF-8で書く
